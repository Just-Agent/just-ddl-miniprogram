<view class="empty">
  <view class="empty__icon">DDL</view>
  <view class="empty__title">{{title}}</view>
  <view wx:if="{{description}}" class="empty__desc">{{description}}</view>
  <button wx:if="{{action}}" class="empty__action" bindtap="handleAction">{{action}}</button>
</view>
